name: nemo_ui_example
publish_to: none
description: Interactive foundation catalog for nemo_ui.
version: 0.0.1

environment:
  sdk: ^3.13.0
  flutter: '>=3.47.0'

dependencies:
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  nemo_ui:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
